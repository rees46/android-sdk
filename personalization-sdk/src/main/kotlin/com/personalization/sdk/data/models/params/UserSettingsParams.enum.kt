package com.personalization.sdk.data.models.params

enum class UserSettingsParams(val value: String) {
    SHOP_ID("shop_id"),
    SHOP_SECRET_KEY("shop_secret"),
    DID("did"),
    SEANCE("seance"),
    SID("sid"),
    SEGMENT("segment"),
    STREAM("stream"),
    SOURCE("source"),
    SOURCE_FROM("from"),
    SOURCE_CODE("code")
}
